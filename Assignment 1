#include <stdio.h>
int main()
{
    int n,flag=0;
    scanf("%d",&n);
    int a[n],i,j;
    for(i=0;i<n;i++)
    {
    scanf("%d",&a[i]);
    }
  for(i=0;i<n;i++)
  {
   for(j=1;j<n;j++)
   {
   if(a[i]!=a[j])
flag++;
  else
 flag=0;
  }
  }
if(flag==0)
printf("UNIQUE");
else if(flag!=0)
printf("DUPLICATE");
}
