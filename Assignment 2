#include<stdio.h>
int fact(int x)
{
if(x==1)
return 1;
else
return x*fact(x-1);
}

void main()
{
int i,n,a;
scanf("%d",&n);
int ar[n];
for(i=0;i<n;i++)
{
scanf("%d",& ar[i]);
}
for(i=0;i<n;i++)
{
a=a+fact(ar[i]);
}
printf("%d",a);
}
